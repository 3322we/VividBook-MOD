%%% ========== 文档配置宏包 ==========
% 自定义宏包请首先加载此宏包
% Elegantbook 模板中已经配置了的命令, 此处全部注释掉. 用其他文档类加载此宏包时, 请注意在必要处取消注释.

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{Macros/Econfig}

%% ======== 定义全局颜色 ======== %%
% 一些自定义宏包用了特殊颜色, 这些颜色定义在 elegantbook 模板中, 如果不使用此模板, 就需要事先定义这些颜色. 
%\RequirePackage[table]{xcolor} %文字颜色
%\RequirePackage{tcolorbox} %文本框颜色
    %\tcbuselibrary{vignette,skins,breakable,theorems}  %tcolorbox 样式选项
%\definecolor{cyan}{RGB}{0,140,210}
%\definecolor{lightergray}{gray}{0.99}
%\definecolor{structurecolor}{RGB}{0,174,247}
%\definecolor{main}{RGB}{70,150,130}
%% ======== 颜色定义完毕 ======== %%

%% ======== 定义字体 ======== %%
% ====== 字体宏包加载 ======
%\RequirePackage[UTF8]{ctex} 如果文档类未支持中文, 则使用此命令
%\RequirePackage[no-math]{fontspec} % 西文字体配置
%\RequirePackage{xeCJK} % 中文字体配置
% ====== 设置西文字体 ======
    % 全局字体
    %\setmainfont{Times New Roman}
    %\setsansfont{TeX Gyre Heros}
    %\setmonofont{TeX Gyre Cursor}

    % 备选字体
    %\newfontfamily{\timesfamily}{Times New Roman}
    %\newfontfamily{\tgtfamily}{TeX Gyre Termes}
    %\newfontfamily{\tgpfamily}{TeX Gyre Pagella}
    %\newfontfamily{\stixtwofamily}{STIX Two Text}
    %\newfontfamily{\concretefamily}{CMU Concrete}
    %\newfontfamily{\garamondfamily}{EB Garamond}
%% ======== 字体定义完毕 ======== %%

%% ======== 文档配置 ======== %%
% ====== 页面样式 ======
%\RequirePackage[a4paper,hmargin=3cm,vmargin=2.5cm]{geometry} 
    %\geometry{
        %a4paper,
        %margin = 2cm
        %hmargin = 2.5cm,
        %vmargin = 2cm
        %} % 调整页边距

% ====== 页眉页脚 ======
%\RequirePackage{fancyhdr} %页眉页脚

% ====== 行距 ======
%\renewcommand{\baselinestretch}{1.2}
  %\setlength{\abovedisplayskip}{3pt}
  %\setlength{\belowdisplayskip}{3pt}
  %\RequirePackage[flushmargin,stable]{footmisc}
  %\setlength{\footnotesep}{12pt}
\newenvironment{controlequation}{\setlength{\abovedisplayskip}{3pt}
\setlength{\belowdisplayskip}{3pt} 
\begin{equation*}
}{
\end{equation*}
}

% ====== 超链接 ======
%\RequirePackage{hyperref}        % 尽量最后加载
    %\hypersetup{
        %colorlinks=true,         % 彩色链接文字, 默认false
        %linkcolor=red,           % 内部链接颜色
        %citecolor=green,         % 引用链接颜色
        %urlcolor=blue,           % URL链接颜色
        %breaklinks=true,         % 允许链接换行
        %}

% ====== 封面 ======
%\RequirePackage{authblk} %封面标题信息增强

% ====== 目录 ======
\setcounter{tocdepth}{2} %目录显示到 subsection 层级 

% ====== 章节标题 ======
\setcounter{secnumdepth}{1} %标题编号只显示到 section 层级, 每节第一个子标题前面要加上命令 \phantomsection.

%% ======== 文档配置完毕 ======== %%

%% ======== 命令配置 ======== %%
% ====== 支持子文档 =======
\RequirePackage{subfiles}

% ====== 随机文本生成 ======
\RequirePackage{lipsum,zhlipsum}

% ====== 自定义高亮 ======
\renewcommand{\emph}[1]{{\color{structurecolor}\heiti#1}}
\newcommand{\fullemph}[1]{
\colorbox{structurecolor!20}{#1}
}
% ====== 允许环境分页显示 ======
\allowdisplaybreaks
%% ======== 命令配置完毕 ======== %%

\endinput